module StepikAlgorithmsAndDataStructures where


--karatsubaMult x y
--  | n == 1 = x * y
--  | otherwise = p1 * 2 ** 2 * (n `div` 2) + (p3 - p1 - p2) * 2 ** (n `div` 2) + p2 
--  where
--    n = max x y
--    (xl, xr) = splitAt (n `div` 2) x   
